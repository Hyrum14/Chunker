1) Reproduce out by one counting error when a word is at the end of the chunk
2) Revise implementation of equal variance alg. to be accurate to the given document "Preliminary Description of Equal Variance Chunking Method 2"
3) meta-data pages
4) Add Standardized Count (Send a copy)
    a) Standardized Count page is ONLY for the new function words
        Columns:
            1) Part of Speech
            2) Phrase/word
            3) "Total of all Chunks" the "row sum" or the total count of each item in the row
                a) NOT the size of the chunk
            4+) count for each chunk

        Row Sections:
            0) details
                a) row 1 is the labels
                b) row 2 is the chunk size row
            1) rawFunctionWords "standardized"
            2) aggregateFunctionWords
            3) specialPhrases
                 no separators or labels
    sections:


     1) Standardized_Count_Factor * (num appearances / total words in the chunk)
     2) Default: Standardized Count Factor = 10,000
     3) include aggregate data

    b) Inverted Standardized Count page (Send another copy)
    Deletions:

        Columns:
            1) Part of Speech // delete
            2) Phrase/word
            3) "Total of all Chunks" the "row sum" or the total count of each item in the row
                a) NOT the size of the chunk
            4+) count for each chunk

        Row Sections:
            0) details
                a) row 1 is the labels
                b) row 2 is the chunk size row // delete
            1) rawFunctionWords "standardized"
            2) aggregateFunctionWords
            3) specialPhrases
            4) delete all rows that have a 0 total


                    Columns:
                        1) Phrase/word
                        2) "Total of all Chunks" the "row sum" or the total count of each item in the row
                            a) NOT the size of the chunk
                        3+) count for each chunk

                    Row Sections:
                        0) details
                            a) row 1 Document names
                            a) row 2 chunk names "label" column
                        1) rawFunctionWords "standardized" (exclude 0 rows)
                        2) aggregateFunctionWords (exclude 0 rows)
                        3) specialPhrases (exclude 0 rows)


Contractions
gui item for the variance factor